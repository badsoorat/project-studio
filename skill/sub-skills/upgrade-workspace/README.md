# upgrade-workspace sub-skill — placeholder

**Status:** stub — body delivered in later phases of the upgrade-system meta-project.

This directory will contain the `upgrade-workspace` sub-skill: a Track-B
deliverable that helps existing downstream `project-studio` workspaces
migrate across version bumps safely.

See `UPGRADE-SYSTEM-PLAN.md` §2.3.9 (atom spec) and §1.2 (release tracks).

Authored by: `downstream-compat-lead` (per `.project-studio/team.yaml`).

Until this directory contains a SKILL.md of its own, the Track B
release workflow (`.github/workflows/release-track-b.yml`) is a no-op
gated by a `skip-if-stub` check.
